# Environment variables for Repair Submission Feature

# Nodemailer (Email Sending)
# SMTP_HOST: Your SMTP server host (e.g., smtp.gmail.com)
# SMTP_PORT: Your SMTP server port (e.g., 587 for TLS, 465 for SSL)
# SMTP_SECURE: Set to 'true' for SSL (port 465) or 'false' for TLS (port 587)
# SMTP_USER: Your SMTP username (e.g., your_email@example.com)
# SMTP_PASS: Your SMTP password or app-specific password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=min.redss@gmail.com
SMTP_PASS=

# Google Sheets API
# GOOGLE_SERVICE_ACCOUNT_EMAIL: The email address of your Google Service Account
# GOOGLE_PRIVATE_KEY: The private key from your Google Service Account JSON file.
#                     Ensure to replace actual newline characters with \n if copying from a single line.
#                     Example: -----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
GOOGLE_SERVICE_ACCOUNT_EMAIL=min.redss@gmail.com
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n\n-----END PRIVATE KEY-----\n