---
import BaseLayout from '@/layouts/BaseLayout.astro';

const pageTitle = "기술 문의 접수 완료";

const url = Astro.url;
const params = url.searchParams;

const customerInfo = {
  companyName: params.get('companyName'),
  name: params.get('name'),
};
---

<BaseLayout title={pageTitle} lang="ko">
  <div class="container mx-auto p-6 text-center">
    <h1 class="text-3xl font-bold mb-4">기술 문의가 성공적으로 접수되었습니다.</h1>
    <p class="text-lg text-gray-700 mb-8">
      {customerInfo.name || '고객'}님의 기술 문의가 접수되었습니다.
      빠른 시일 내에 담당자가 확인 후 연락드리겠습니다.
    </p>
    <a href="/ko" class="inline-block bg-blue-600 text-white font-semibold py-3 px-6 rounded-md hover:bg-blue-700 transition-colors">
      홈으로 돌아가기
    </a>
  </div>
</BaseLayout>